<h1 class="font-bold text-4xl text-center text-accent mb-6">
  <i>BLOG</i>
</h1>

<div class="mx-auto max-w-max m-8 lg:px-36">
  <%= render(TagCloudComponent.new(tags: @tags)) %>
</div>

<%= render(PostComponent.with_collection(@posts)) %>