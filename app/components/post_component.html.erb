<%= link_to @post do %>
  <div class="my-6 lg:px-24">
    <p class="text-sm opacity-70">
      <%= @post.date.strftime("%d/%m/%Y") if @post.date %>
    </p>
    <h2 class="text-3xl font-bold hover:underline">
      <%= @post.title %>
    </h2>
    <div class="my-3">
      <%= render(TagListComponent.new(tags: @post.tag_list)) %>
    </div>
    <p class="my-8 first-line:uppercase first-line:tracking-widest first-letter:text-5xl first-letter:font-bold first-letter:mr-3 first-letter:float-left">
      <%= truncate(@post.content,length: 220) %>
    </p>
    <div class="divider"></div> 
  </div>
<% end %>